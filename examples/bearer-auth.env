# Vault with bearer token authentication
# Clients must send: Authorization: Bearer <token>
MARKDOWN_VAULT_MCP_SOURCE_DIR=/data/vault
MARKDOWN_VAULT_MCP_BEARER_TOKEN=change-me-to-a-random-secret

# Optional: restrict to read-only
# MARKDOWN_VAULT_MCP_READ_ONLY=true
